import numpy as np;
from matplotlib import pyplot as plt;
x=[];
y=np.random.uniform(-100,100,200));
for i in range(-100,100):
    x.append(i);
plt.plot(x,y);
plt.scatter(x,y,color='red');
plt.show();
